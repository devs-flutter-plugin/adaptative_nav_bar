name: adaptative_nav_bar_example
description: Example application for adaptative_nav_bar.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  adaptative_nav_bar:
    path: ..
  flutter:
    sdk: flutter
  go_router: ^18.0.1
  material_ui: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
